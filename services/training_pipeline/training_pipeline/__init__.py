"""Training pipeline (batch job).

Consumes processed features + labels and produces a trained model + metrics.
"""

"""Training pipeline (batch job).

Consumes processed features + labels and produces a trained model + metrics.
"""

